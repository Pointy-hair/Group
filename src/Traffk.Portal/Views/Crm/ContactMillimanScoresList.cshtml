@using static TraffkPortal.Controllers.CrmController
@model IEnumerable<MillimanScore>
@{
    Layout = "~/Views/Crm/_LayoutContact.cshtml";
    ViewData.IsListingPage(true);
    ViewData.SetTitleAndHeading(CrmController.ViewNames.Health.MillimanScoreList.ToCrmHeading());
    ViewData.HasPageMenuItems(false);

}
@section listingTable
    {
<table class="table table-bordered table-hover table-striped">
    <thead>
    <tr>
        @*<th>
            @Html.SortableHeaderFor(z => z.dw_record_id)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.score_start_date)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.score_end_date)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.total_score)
        </th>*@
        <th>
            @Html.SortableHeaderFor(z => z.MillimanScoreId)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.ScorePeriodStartDd)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.ScorePeriodEndDd)
        </th>
        <th>
            @Html.SortableHeaderFor(z => z.TotalScore)
        </th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    <a asp-action="@ActionNames.Health.HistoricalScore" asp-route-id="@ViewContext.GetRouteValue()" asp-route-recordId="@item.dw_record_id">@item.dw_record_id</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.score_start_date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.score_end_date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.total_score)
                </td>*@
                <td>
                    <a asp-action="@CrmController.ActionNames.Health.MillimanScore" asp-route-id="@ViewContext.GetRouteValue()" asp-route-recordId="@item.MillimanScoreId">@item.MillimanScoreId</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ScorePeriodStartDd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ScorePeriodEndDd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalScore)
                </td>
            </tr>
        }
    </tbody>
</table>

}
