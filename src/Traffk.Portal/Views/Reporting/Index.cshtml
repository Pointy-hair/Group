@using RevolutionaryStuff.PowerBiToys.Objects
@using RevolutionaryStuff.Core.Collections
@using RevolutionaryStuff.Core.ApplicationParts
@using Traffk.Tableau.REST.Models
@model TreeNode<Traffk.Tableau.REST.Models.SiteViewResource>
@{
    Layout = "~/Views/Shared/_LayoutListing2.cshtml";
    ViewData.SetTitle("Reporting");
    var filters = ViewBag.ListingFilters as ListingFilters;
    var hasFilters = filters != null;
}

<!-- Content: Reports -->
<section id="reports-panel">
    <div class="row">
        <div class="col-xs-12">
            <h1>Reports</h1>
            <p class="subheading">You're viewing the reports screen.</p>
        </div>
    </div>
    <div class="row">
        @await Html.PartialAsync("_ReportFiltersPartial2", filters)
        @*@if (hasFilters)
        {
            @await Html.PartialAsync("_ReportFiltersPartial2", filters)
        }*@
        <div class="col-sm-8" id="filter-tags-container">
            <ul class="filter-tags">
                <li><a href="#">Workers Comp (3) <span><i class="fa fa-times" aria-hidden="true"></i></span></a></li>
                <li><a href="#">Risk Reports (3) <span><i class="fa fa-times" aria-hidden="true"></i></span></a></li>
            </ul>
        </div>
        <div class="col-sm-2 text-right">
            <button class="btn btn-large btn-white toggle" data-toggle="actions-filter">Actions</button>
            <ul id="actions-filter" class="floated-filter list-right">
                <li class="multiselect-action"><a href="#" class="popup-confirm_open">Export</a></li>
                <li><a href="#">Delete</a></li>
                <li><a href="#">View</a></li>
            </ul>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12">
                @{ await Html.RenderPartialAsync("_TableauViewResourceTreeNodePartial2", Model.Children); }
        </div>
    </div>
</section>
<!-- /end Content: Reports -->


